build/
rogatia
rogatia-debug
*.o
*.d
*.exe

# NNUE nets are large binaries; they are released as GitHub release assets,
# not committed. EVALFILE points at a local path.
*.nnue
*.bin

# Datagen output (Phase 5) -- hundreds of GB, never committed.
data/
*.viriformat
*.binpack
*.zst

# Test artifacts
*.pgn
*.epd
!tests/*.epd
logs/
sprt-results/

# Testing harness: fastchess, opening books, reference engines. Fetched by
# scripts/setup-testing.sh, never committed -- the UHO book alone is 175 MB and
# anything committed to git history is effectively permanent.
tools/
books/
config.json

.vscode/
.idea/
.DS_Store
compile_commands.json
rogatia-base
rogatia-phase4
sprt-results/
books/
tools/
data/
nets/
rogatia-dev
rogatia-nnue
rogatia-hce
